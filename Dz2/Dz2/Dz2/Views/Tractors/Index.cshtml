@model IEnumerable<Dz2.Models.Tractor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.model_name_tractor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.serial_number_tractor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.model_name_tractor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.serial_number_tractor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TractorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TractorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TractorID })
        </td>
    </tr>
}

</table>
